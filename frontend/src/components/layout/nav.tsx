'use client'; import Link from 'next/link'; import { usePathname } from 'next/navigation'; import type { Locale } from '@/types/api'; import { t } from '@/lib/i18n/dictionaries';
function toggleTheme(){document.documentElement.classList.toggle('dark'); localStorage.setItem('theme',document.documentElement.classList.contains('dark')?'dark':'light')}
export function Nav({locale}:{locale:Locale}){const path=usePathname(); const other=locale==='ar'?'en':'ar'; const otherPath=path.replace(`/${locale}`,`/${other}`); const items=['products','cart','orders','admin','delivery'] as const; return <header className="card" style={{margin:'18px auto',maxWidth:1200,display:'flex',gap:16,alignItems:'center',justifyContent:'space-between'}}><Link href={`/${locale}`} style={{fontWeight:900,fontSize:22}}>{t(locale,'brand')}</Link><nav style={{display:'flex',gap:12,flexWrap:'wrap'}}>{items.map(i=><Link key={i} href={`/${locale}/${i}`}>{t(locale,i)}</Link>)}</nav><div style={{display:'flex',gap:8}}><Link className="btn secondary" href={otherPath}>{other.toUpperCase()}</Link><button className="btn secondary" aria-label="toggle theme" onClick={toggleTheme}>◐</button></div></header>}
