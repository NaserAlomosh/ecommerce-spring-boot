'use client'; import { register } from '@/features/auth/auth-actions'; import { Input } from '@/components/ui/input'; import { Button } from '@/components/ui/button'; export default function Register(){return <section className="container"><form className="card grid" onSubmit={async(e)=>{e.preventDefault(); const f=new FormData(e.currentTarget); await register(Object.fromEntries(f) as Record<string,string>)}}><h1>Register / إنشاء حساب</h1>{['firstName','lastName','email','phoneNumber','password'].map(n=><Input key={n} name={n} type={n==='password'?'password':'text'} placeholder={n}/>) }<Button>Create account</Button></form></section>}
