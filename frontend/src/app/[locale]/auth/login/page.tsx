'use client'; import { login, googleLogin } from '@/features/auth/auth-actions'; import { Input } from '@/components/ui/input'; import { Button } from '@/components/ui/button'; import { useState } from 'react';
export default function Login(){const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[google,setGoogle]=useState('');return <section className="container"><form className="card grid" onSubmit={(e)=>{e.preventDefault(); void login(email,password)}}><h1>Login / تسجيل الدخول</h1><Input placeholder="email" value={email} onChange={e=>setEmail(e.target.value)}/><Input placeholder="password" type="password" value={password} onChange={e=>setPassword(e.target.value)}/><Button>Login</Button><hr/><Input placeholder="Google Gmail identity token" value={google} onChange={e=>setGoogle(e.target.value)}/><Button type="button" onClick={()=>void googleLogin(google)}>Continue with Gmail</Button></form></section>}
