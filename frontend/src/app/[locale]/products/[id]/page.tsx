import { asLocale } from '@/lib/i18n/routing'; import { productService } from '@/features/products/product-service';
export default async function ProductDetails({params}:{params:{locale:string;id:string}}){const locale=asLocale(params.locale); const product=await productService.get(params.id); return <section className="container grid"><article className="card"><div style={{height:320,borderRadius:22,background:'linear-gradient(135deg,var(--brand),var(--brand2))'}}/><h1>{locale==='ar'?product.nameAr:product.nameEn}</h1><p className="muted">{product.sku}</p><h2>{product.effectivePrice??product.price} {product.currency}</h2><p>{product.inStock?'In stock':'Out of stock'} · ★ {product.averageRating??0} ({product.reviewsCount??0})</p><button className="btn">Add to cart / أضف للسلة</button></article></section>}
