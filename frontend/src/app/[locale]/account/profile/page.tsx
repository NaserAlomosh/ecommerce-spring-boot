import { ResourcePage } from '@/features/admin/resource-page'; import { endpoints } from '@/lib/api/endpoints'; export default function Page(){return <ResourcePage title="Customer Profile" url={endpoints.users + '/me'} actions/>}
