spring:
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:${MYSQL_PORT:3306}/${MYSQL_DATABASE:ecommerce_dev}?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: ${MYSQL_USER:ecommerce_user}
    password: ${MYSQL_PASSWORD:ecommerce_password}
  jpa:
    show-sql: true

app:
  flyway:
    repair-v19-checksum: ${FLYWAY_REPAIR_V19_CHECKSUM:true}
